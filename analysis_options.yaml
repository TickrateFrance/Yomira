include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart" # generated (isar) — uses experimental APIs, not our concern

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: false
